# Scanner Bump: scorecard

- Tool: scorecard
- Previous version: 5.0.0
- New version: 5.5.0
- Registry: `ghcr.io/ossf/scorecard` (was `gcr.io/openssf/scorecard`; GCR now requires GCP billing)
- Pin style: tag + lock digest (`mutableSource: true`), same as Semgrep

Validation:

- Run `make scanners-validate`.
- Run `make scanners-upstream-check`.
