# golangci-lint v2 (required by golangci-lint-action v7).
version: "2"

linters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
      - internal/scannerbuild/context/
