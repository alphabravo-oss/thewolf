# ============================================================
# wolf-scanners — pinned tool versions
# ============================================================
# This file is the SOURCE OF TRUTH for every scanner version that
# ships in wolf-scanners. Install scripts source this file; the
# smoke test asserts each tool reports the pinned version.
#
# When bumping a version:
#   1. Update the variable below.
#   2. Run `make scanners-build && make scanners-smoke`.
#   3. Re-run plugin parity tests (any plugin whose tool changed
#      may need its expected-output fixture refreshed).
#   4. Note the bump in CHANGELOG.md.
# ============================================================

# --- Python ecosystem (pip) ---
SEMGREP_VERSION=1.92.0
BANDIT_VERSION=1.7.10
RUFF_VERSION=0.7.1
MYPY_VERSION=1.13.0
PIP_AUDIT_VERSION=2.7.3
RADON_VERSION=6.0.1
VULTURE_VERSION=2.13
DETECT_SECRETS_VERSION=1.5.0
CHECKOV_VERSION=3.2.297
SCANCODE_VERSION=32.3.0
SQLFLUFF_VERSION=3.2.5

# --- Go ecosystem (go install) ---
GOSEC_VERSION=2.21.4
STATICCHECK_VERSION=2024.1.1
GOVULNCHECK_VERSION=1.1.3

# --- Node ecosystem (npm -g) ---
ESLINT_VERSION=9.13.0
SPECTRAL_VERSION=6.13.1

# --- Rust ecosystem (rustup) ---
RUST_TOOLCHAIN=1.82.0

# --- Ruby ecosystem (gem) ---
BRAKEMAN_VERSION=6.2.2
RUBOCOP_VERSION=1.66.1

# --- PHP ecosystem (composer) ---
PHPSTAN_VERSION=1.12.7

# --- Java/Native (github release tars + apt) ---
INFER_VERSION=1.2.0
PMD_VERSION=7.7.0

# --- GitHub release tarballs ---
TRIVY_VERSION=0.57.0
GITLEAKS_VERSION=8.21.2
TRUFFLEHOG_VERSION=3.83.5
HADOLINT_VERSION=2.12.0
DOCKLE_VERSION=0.4.14
TFLINT_VERSION=0.54.0
KUBESCAPE_VERSION=3.0.22
KUBE_LINTER_VERSION=0.7.1
SYFT_VERSION=1.17.0
GRYPE_VERSION=0.84.0
OSV_SCANNER_VERSION=1.9.1
VALE_VERSION=3.9.1
NUCLEI_VERSION=3.3.5
SWIFTLINT_VERSION=0.57.0
CODEQL_VERSION=2.19.3
SCORECARD_VERSION=5.0.0
